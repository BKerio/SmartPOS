# Enrollment PC — run: dotnet run (or ./run-linux.sh)
FINGERPRINT_PORT=17890
FINGERPRINT_BIND=*
# Comma-separated browser origins allowed to call this service directly (optional if using backend proxy)
FINGERPRINT_CORS_ORIGIN=http://localhost:5173,https://betterfork.millenium.co.ke
